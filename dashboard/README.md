# Dashboard Placeholder

The hosted dashboard is intentionally deferred in the recommended Plan A build.

This directory exists now so the monorepo shape is stable, but no dashboard implementation is expected until the roadmap explicitly moves beyond the local-first OSS core.
